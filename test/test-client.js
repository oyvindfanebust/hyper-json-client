var assert = require('assert');
var client = require('../src/client');

describe('calling a root url', function () {
	it('should return resource', function () {
		// var result;
		// client('http://localhost:1234/api')
		// 	.success(function(resource) {
		// 		result = resource;
		// 	});

		// assert.equals();
	});
});